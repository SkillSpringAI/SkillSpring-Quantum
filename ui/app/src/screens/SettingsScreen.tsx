export default function SettingsScreen() {
  return (
    <section className="panel">
      <h2>Settings</h2>
      <p className="muted">
        App preferences, output roots, desktop bridge config, and future shell settings should surface here.
      </p>
    </section>
  );
}
