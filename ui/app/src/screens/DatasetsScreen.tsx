export default function DatasetsScreen() {
  return (
    <section className="panel">
      <h2>Datasets</h2>
      <p className="muted">
        Versioned dataset browsing, manifests, filters, and export subsets should surface here.
      </p>
    </section>
  );
}
